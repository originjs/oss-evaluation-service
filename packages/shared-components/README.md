# @originjs/oss-evaluation-components

共享组件包，提供前端复用的组件、样式和相关能力。

## 作用

该包用于承载可复用的 Vue 组件与前端基础能力，供站点或其他前端包共享使用。

## 常用命令

```bash
pnpm -C packages/shared-components build
pnpm -C packages/shared-components test
```

对应含义：

- `build`：执行类型检查并构建组件产物
- `test`：运行 Vitest 测试
