# shared-components style

前端共享样式子包。

## 作用

该目录承载共享的 Less 样式入口，目前主要通过 `index.less` 对外提供基础样式能力。

## 包名

- `@orginjs/oss-evaluation-components-style`

## 使用方式

该子包作为 `shared-components` 内部样式模块存在，供前端构建和组件样式复用。
