# util

通用工具函数包。

## 作用

该包提供多个服务共享使用的基础工具与常量，目前导出内容包括：

- 日期处理工具
- `Result` 结果封装
- 周序号工具
- 常量定义

## 构建命令

```bash
pnpm -C packages/util build
```

该命令会执行 TypeScript 编译，产出 `dist/`。

## 使用方式

这是一个被其他包依赖的基础工具包，本身不作为独立服务运行。
