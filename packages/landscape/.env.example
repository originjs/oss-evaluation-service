# landscape update 环境变量示例（只适用于 packages/landscape update）
# 执行 pnpm -C packages/landscape update 时使用。

# 必填：主 MySQL 连接地址。
DATABASE_URL=mysql://oss_eval_user:change_me@mysql-host.example.com:3306/oss-eval

# 运行模式。
NODE_ENV=development

# 日志目录。
LOG_DIR=logs

# 可选：第二个数据库连接。不使用就保持空。
DATABASE_EXT_URL=
